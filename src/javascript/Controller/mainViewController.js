

// これから作ります
var test;

// ログは汎用的に使わないで、できるだけ専用のログの変数をつくる。(汎用的にするのはめんどい)
var log;

// メッセのログ
var logMessage;

// キャラ名のログ
var logCharactorName;

// 状態のログ
var logStatus;

// フラグのろぐ
var logFlag;

// データベースのkeyはスネークケースで書く
var char_id;

var dere_flag;
var tsun_flag;
var tsun_dere_flag;
var hoge_flag;
var char_status;
var hoge_status;

function logMessage (argument) {
  // body...
}

function getLog (argument) {
  // body...
}

function getCharactorController (argument) {
  // body...
}